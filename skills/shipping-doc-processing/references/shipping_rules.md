# 船务清单处理规则

## 数据源优先级

1. 流转单内容索引
2. 订单映射表
3. 原始流转单 workbook + sheet

## EPL / PL 填写规则

- `Dimension` 使用流转单外箱规格和外箱总数
- `C/N` 根据外箱总数生成，如 `1-2`
- 箱数使用流转单外箱需求数量
- 配件若与上一主产品共箱，可不单独重复填写 `C/N / G.W. / Dimension`

## 未命中处理

- 如果内容索引未命中：
  - 再查订单映射表
- 如果映射表也未命中：
  - 明确告诉用户当前索引和映射都未找到对应订单
